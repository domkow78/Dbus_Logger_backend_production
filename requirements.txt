pyserial
crcmod
pytest
pytest-cov
fastapi
uvicorn[standard]
websockets
requests
luma.oled
smbus2
Pillow

# Optional: For auto-discovery of stations in LAN (mDNS)
# Uncomment if you want to enable auto-discovery feature
# zeroconf